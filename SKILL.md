---
name: requirement-solution-planner
description: 将模糊想法、需求、优化建议或实施请求整理成可执行、可验证、可复核的方案。用在用户想先做需求拆解、实施规划、技术方案、路线图、里程碑、任务分解、优先级、排期、交付物、验收标准、风险识别、复核机制、上线形态与生产地基判断、领域建模与架构预判，而不是立刻编码或开工时。适用于“先给方案再执行”“先别写代码先分析”“先帮我梳理一下”“拆成可执行步骤”“给我实施方案”“做个路线图”“排个优先级”“拆成里程碑”“这是一个需求还是多个需求”“按总方案继续拆”“执行 R1/开始第 1 步”等请求。优先暴露假设和歧义，必要时先追问，再把单/多需求改写成可交接的方案、PRD/issue brief 或执行切片。
---

# Requirement Solution Planner

先把输入整理成可交付、可验证、可复核的方案，再决定是否进入执行。

## 执行顺序

始终按这个顺序处理：

1. 写方案前先思考
2. 判断是否需要先追问
3. 判断是单需求还是多需求
4. 判断上线形态并列出生产地基要求
5. 定义交付物和成功标准
6. 拆成可验证步骤或可交接事项
7. 为每一步补复核机制
8. 判断是否需要沉淀 `CONTEXT.md`、ADR、issue brief 或执行 handoff

如果是多需求，必须按“分批文件结构”输出：先给 `00-分批方案索引.md`，再按用户要求生成 `R1-xxx方案.md`、`R2-xxx方案.md`、`R3-xxx方案.md` 等独立文件。不要把所有 R 项塞进一篇大文档。

## 基本规则

- 不要在未完成结构化之前直接开始写代码、PRD、SQL、文案或实现细节，除非用户明确要求执行某一步。
- 不要接受“优化一下”“完善一下”这类模糊交付。必须改写成明确交付物、完成标准、错误判断标准、范围边界。
- 如果存在多个合理解释，不要静默选择其中一个；要显式写出判断依据、默认假设或备选路径。
- 每个步骤必须可独立验证，且一次只做一件事。
- 每完成一步都要给出复核结论：`继续`、`返工` 或 `暂停`。
- 信息不完整时，不要卡住；先列出已知信息、缺失信息、默认假设、假设风险，再给临时方案。
- 方案若依赖第三方平台（推送、支付、OAuth、云服务、消息通道等），必须在交付物定义前列出该平台的接入前提（所需账户类型、认证方式、是否需要审核/付费/企业认证）。不确定时必须注明“未验证”，并建议先验证再开工。
- 如果缺失信息会改变交付物、优先级、技术路线或范围边界，先进入追问模式，不要急着写完整方案。
- 如果用户要求”执行 R1””开始第 1 步””按方案继续做”，先读取或复述对应方案边界，再进入执行接力，不要重新发散需求。
- 每个方案必须显式标注上线形态（原型 / 内部工具 / 生产系统）；生产系统不允许把地基项写进”暂不包含”，只能”先做最小版本”。
- 每条完成标准与错误判断标准必须对应至少一个复核动作；复核方式未覆盖某条标准时，方案不算完成。
- 方案会交给 AI 执行时，结构红线（分层、异步、前端路由与请求层、单文件规模）必须直接写进完成标准和错误判断标准，不能靠执行者自觉。
- 每个 R 项必须逐项回答横切关注点检查表（认证授权、密钥、幂等重试、事务边界、错误分类、日志、迁移回填、框架复用）。

## 高频触发说法

以下表达通常都应触发这个 skill：

- “先别做，先帮我梳理一下”
- “先给我一份方案”
- “先分析再做”
- “先别写代码，先拆需求”
- “拆成可执行步骤”
- “给我一个实施方案 / 技术方案 / 路线图”
- “这些需求怎么排优先级 / 怎么分批 / 怎么排期”
- “帮我拆成里程碑 / 任务清单 / 阶段计划”
- “按总方案继续拆”
- “这是一件事还是多件事”
- “开始做 R1 / 执行第 1 步”
- “把这个方案转成 issue / PRD / 交接文档”

## 三条核心护栏

### 1. 写方案前先思考

不要妄下断言。不要掩饰困惑。坦诚地权衡利弊。

- 明确陈述假设：如果不确定，请询问而不是猜测。
- 提出多种解释：当存在歧义时，不要默默地做出选择。
- 必要时提出异议：如果存在更简单的方法，就说出来。
- 感到困惑时停下来：说出不清楚的地方并请求澄清。

在输出方案前，先完成这组检查：

- 已知信息是什么
- 缺失信息是什么
- 当前采用了哪些默认假设
- 是否存在多种解释
- 选择当前方案的理由是什么
- 有没有更简单的方案
- 方案是否依赖第三方平台/API/服务？如是，未查证其接入前提（账户类型、认证方式、付费计划、权限级别、速率限制）就不得假设”有账号就能用”
- 这个系统的上线形态是什么（原型 / 内部工具 / 生产系统）？生产系统必须过地基清单
- 未来会扩展哪些模块？哪些核心实体会被下游反复引用（应建模为外键而非字符串快照）？哪些演进已确定会发生（应做最低成本一等化而非硬编码）
- 哪些假设是”地基级假设”（错了要整体返工：认证方式、主数据模型、模块边界）？必须显式标记并写明验证动作，未验证不得直接进入执行

补充规则：当方案涉及团队或 AI 均无先前经验的第三方平台/服务时，必须显式标注为“平台知识风险”，并在方案中要求查阅官方文档验证准入前提。不得在未验证准入条件的情况下直接进入实现步骤。

### 2. 简单至上

用最少的方案解决当前问题。不要进行没有依据的推测。

- 除了用户要求的目标外，不额外增加功能。
- 不为一次性事项设计抽象体系。
- 不提供未要求的“灵活性”或“可配置性”。
- 不为极低概率或不可能的情况增加复杂处理。
- 例外：已确定会发生的演进不算投机设计——未来确定需要 且 现在实现成本低 → 现在做最低成本一等化（如核心实体外键、site 实体化）；未来不确定 或 成本高 → 不做。
- 如果 15 个步骤可以缩减到 5 个步骤，就重写它。

测试标准：一位资深工程师会认为这套方案过于复杂吗？如果会，请简化。

### 3. 目标驱动型执行

定义成功标准。循环直至验证通过。

把紧迫或模糊的任务转化为可验证目标：

- “添加验证” -> “编写针对无效输入的测试，然后确保它们都能通过。”
- “修复漏洞” -> “编写一个能够重现该问题的测试，然后让它通过。”
- “重构 X” -> “确保重构前后测试均通过。”

对于多步骤任务，使用这种结构：

```text
1. [Step] -> verify: [check]
2. [Step] -> verify: [check]
3. [Step] -> verify: [check]
```

严格的成功标准能让执行者独立推进；宽松标准，例如“只要能运行就行”，需要继续澄清。

## 上线形态与生产地基

每个方案必须显式标注上线形态：`原型` / `内部工具` / `生产系统`。判断依据：是否会承载真实用户与真实数据、是否要上线运行、是否进入公司正式流程。上线形态会改变地基要求，也改变"暂不包含"允许写什么。

### 生产系统地基清单

以下项是生产系统地基，只能"先做最小版本"，不允许写进"暂不包含"：

- 认证与授权模型：未认证访问敏感数据视为失败，权限矩阵至少覆盖最小角色集
- 密钥/凭证治理：无默认值、只从环境变量或密钥管理读取、泄露即轮换、不进入版本库
- 生产部署形态：不用开发服务器、`DEBUG=0`、强随机口令、DB/Redis 端口不外露、健康检查、迁移不竞争
- 核心主数据建模：下游用外键引用核心实体，不用字符串快照拼装
- 审计日志：谁在什么时间做了什么
- 可观测性：依赖探活 + 结构化日志
- 备份与恢复策略

"暂不包含"只能收功能范围；若必须推迟某项地基，要显式写出被接受的剩余风险并让负责人知情。

### 结构红线（AI 执行时写进验收标准）

- 视图只做 HTTP 编解码与编排；业务逻辑进 service 层；模型不写业务。
- 长任务一律异步；必须定义重试策略、幂等键、精确异常分类（禁止吞 `Exception`）。
- 前端必须有路由与统一请求层（token 注入、401/403 处理、统一错误提示）；单文件行数进入完成标准。
- 不重复造框架轮子：Django Admin、i18n、DRF 权限等框架能力优先于手写实现。

### 地基级假设

"写方案前先思考"里的假设必须分级。认证方式、主数据模型、模块边界属于地基级假设——错了要整体返工。地基级假设必须显式标记、写明验证动作，未验证不得直接进入执行；非地基级假设可以按常规临时方案处理。

## 分支能力

按输入选择必要分支，不要每次全部展开。

### 追问模式

当目标、用户、交付物、范围边界、优先级或平台准入前提不清楚，且这些信息会改变方案方向时，先追问。一次只问一个关键问题，并给出推荐答案。追问规则见 [references/grilling-and-clarification.md](references/grilling-and-clarification.md)。

### 长期上下文沉淀

当方案产生新的项目术语、核心对象、流程状态、架构边界或不可轻易逆转的技术决策时，建议更新 `CONTEXT.md` 或新增 ADR。上下文和 ADR 写法见 [references/context-and-adr.md](references/context-and-adr.md)。

### 方案转执行

当用户要求把方案继续变成 PRD、issue、执行 brief、handoff，或要求执行某个 R 项时，使用方案转执行规则。执行前必须确认目标、范围、验证信号和回滚/返工条件。规则见 [references/execution-bridge.md](references/execution-bridge.md)。

### 工程验证

当需求属于功能开发、Bug 修复、重构、性能优化、界面实现或可交互流程时，优先把成功标准落到测试、复现脚本、浏览器自测或可观察指标上。工程验证规则见 [references/engineering-validation.md](references/engineering-validation.md)。

## 单需求与多需求判断

优先判断输入属于哪一类。

### 单需求

满足任一特征时，按单需求处理：

- 只有一个明确目标
- 只有一个主要交付物
- 只有一条核心执行链路
- 其余内容只是补充说明，不构成独立任务

### 多需求

满足任一特征时，按多需求处理：

- 一次提出多个功能、优化、修复、重构或文档事项
- 同时存在多个独立交付物
- 各事项可以拆成不同执行链路
- 用户明确要求排优先级、分批次、看依赖、继续拆分

如果边界模糊，默认先按多需求拆分，再决定是否合并。

输出开头必须显式写清：

- 需求模式：`单需求` / `多需求`
- 判断依据
- 关键假设
- 是否存在独立事项

## 边界案例

遇到下面这些情况时，按以下方式处理：

- 一个目标，但包含多个明显独立交付物：按多需求处理。
- 一个目标，只有一条主执行链路，但步骤较多：仍按单需求处理，不要因为步骤多就误判为多需求。
- 同时包含“功能 + 文档 + 测试 + 上线”：如果这些只是同一事项的配套动作，按单需求；如果能拆成独立负责人或独立交付物，按多需求。
- 用户丢来很多想法，没有明确主目标：先按多需求拆分，并在开头标注“目标尚未收敛”。
- 用户说“顺手优化一下”“有空也一起做”：默认降级为备注或候选事项，除非它已经构成独立交付物。
- 用户只让你判断需不需要拆开：只做模式判断和拆分依据，不展开完整方案。

## 单需求模式

单需求输出必须覆盖以下部分：

- `0. 需求模式判断`
- `1. 写方案前思考`
- `2. 定义交付物`
- `3. 任务切片`
- `4. 复核机制`

字段模板见 [references/templates.md](references/templates.md) 的“单需求模板”。

### 单需求约束

- 先定义目标交付物，再写步骤。
- 完成标准与错误判断标准必须同时存在。
- 范围边界里要明确包含项、不包含项、完成截止点。
- 每个 Step 都要包含：目标、输入、动作、输出、验证方式、通过条件、不通过处理。
- 如果步骤过多，先尝试合并到最小可执行路径，再保留必要验证点。

## 多需求模式

多需求必须采用“两阶段产出”。

### 阶段 A：分批方案索引

先生成 `00-分批方案索引.md`，用于：

- 拆分独立事项
- 标记类型、优先级、依赖、风险
- 检查冲突与重叠
- 规划批次与总体路线图
- 列出每个 R 文件的文件名
- 给出推荐实施顺序和最小开发闭环
- 判断上线形态（原型 / 内部工具 / 生产系统）
- 识别领域与架构地基：核心实体/主数据、模块边界草案、确定性演进清单
- 定义全局约束：安全模型、数据模型原则、分层规则、任务可靠性规则、部署形态
- 规划地基批次与批次复核闸门：生产系统必须先完成认证、密钥、主数据、模块边界、部署基线并验收，再进功能批次

### 阶段 B：R 项独立方案文件

在索引基础上，把每个 `R1 / R2 / R3 ...` 当成单需求，分别展开成独立方案文件。

不要在阶段 B 重新发散出新的事项体系；独立方案必须继承索引里的目标、边界、优先级、依赖关系。

默认先停在阶段 A，除非用户明确要求“展开全部”“生成所有 R 文件”或指定要展开的 R 项。这样可以先校准拆分，再避免一次性产出过量方案。

### 多需求默认文件结构

多需求默认使用以下文件结构。`xxx` 由具体事项标题替换，文件名要短、清楚、按 R 编号排序：

```text
00-分批方案索引.md
R1-xxx方案.md
R2-xxx方案.md
R3-xxx方案.md
R4-xxx方案.md
R5-xxx方案.md
```

示例：

```text
00-分批方案索引.md
R1-后端工程基线方案.md
R2-考勤核心数据模型方案.md
R3-统一考勤入库服务方案.md
R4-ExcelSource导入能力方案.md
R5-考勤查询API方案.md
R6-Vue前端页面方案.md
R7-异步任务导入审计与错误报告方案.md
R8-未来PushSource预留方案.md
R9-部署测试与验收闭环方案.md
```

如果用户没有要求写入文件，可以在回答中按“文件名 + 文件内容”分块输出。如果用户要求放到某个目录，必须创建该目录，并把索引和指定 R 项分别写成独立 Markdown 文件。

### 分批文件内容要求

`00-分批方案索引.md` 必须包含：

- 总目标
- 上线形态判断（原型 / 内部工具 / 生产系统）
- 技术栈与方案前提（含生产形态必答项：prod 配置、反向代理拓扑、静态/媒体文件策略、迁移策略）
- 领域与架构地基（核心实体/主数据、模块边界、确定性演进清单、地基级假设）
- 全局约束（安全模型、数据模型原则、分层规则、任务可靠性、部署形态）——所有 R 文件必须继承
- 分批文件列表
- 推荐实施顺序（生产系统必须含地基批次）
- 最小开发闭环
- 批次复核闸门（每批结束做安全/架构/数据完整性复核）
- 总体依赖和风险摘要

每个 `R*-xxx方案.md` 必须包含：

- `0. 需求模式判断`
- `1. 小需求`
- `2. 目标交付物` 或该 R 项的核心设计
- `3. 完成标准`
- `4. 错误判断标准`
- `5. 范围边界`
- `6. 横切关注点检查表`（认证授权、密钥、幂等重试、事务边界、错误分类、日志、迁移回填、框架复用；逐项回答：已覆盖 / 不适用 / 留待后续+风险）
- `7. 复核方式`（必须覆盖每一条完成标准与错误判断标准）
- `8. 全局约束继承`（声明引用索引中的哪几条全局约束）

字段模板见 [references/templates.md](references/templates.md) 的“多需求模板”。

## 用户意图映射

按用户表达选择输出深度：

- 用户说“给我一份方案”“先别做，先拆解”“先规划”：单需求给单需求方案；多需求默认先输出 `00-分批方案索引.md`，除非用户要求展开全部。
- 用户说“先做技术方案”“先做实施方案”“先给路线图”“拆成里程碑”：默认输出完整规划，不直接进入实现。
- 用户说“这是一个需求还是多个需求”“先帮我判断需不需要拆开”：只做模式判断、事项拆分和判断依据，不展开完整方案。
- 用户说“先排优先级”“这些先做哪个”“怎么分批”：优先输出多需求模式中的拆分、优先级与批次规划。
- 用户说“先排期”“先列阶段计划”“先出任务清单”：优先输出拆分、批次、依赖和推荐顺序。
- 用户说“根据总方案继续拆”“把 R1 到 R6 分别展开”：直接生成或更新对应 `R*-xxx方案.md`，不重新生成事项体系。
- 用户说“把这个方案转成 PRD / issue / 开发任务”：输出可交接 brief，不新增未经确认的需求。
- 用户说“开始做第 1 步”“执行 R2”“继续下一步”：先进入执行接力，只执行被指定的步骤或事项，并在结尾补复核。
- 用户说“复核一下”“继续前先检查”：只做复核，不继续执行。
- 用户说“帮我沉淀一下”“更新项目上下文”“记录决策”：优先输出 `CONTEXT.md` 更新建议或 ADR 草案。

## 输出风格

- 使用简洁、结构化中文。
- 多用短标签和直接表达。
- 避免空话、套话、泛泛管理术语。
- 让输出可以被另一个 AI 或人直接接手执行。

## 质量红线

不要出现以下情况：

- 未判断单需求/多需求就直接开始规划。
- 未说明假设就直接给结论。
- 把多个事项强行写成一条单链路。
- 多需求下只列 `R1/R2/R3` 概要就结束，未生成索引和独立 R 文件。
- 未定义交付物就直接给最终实施内容。
- 一个步骤塞入多个动作。
- 给出不可验证的步骤。
- 不做复核就进入下一步。
- 已有索引或总方案却不基于它继续拆独立 R 方案。
- 需求关键歧义会改变方向，却没有先追问。
- 执行 R 项前没有确认对应方案边界和验证信号。
- 产生核心术语或架构决策，却没有提示是否沉淀到 `CONTEXT.md` 或 ADR。
- 为了显得完整而加入用户没有要求的功能、配置、抽象或未来扩展。
- 方案涉及第三方推送/支付/登录/API/消息通道，但未列出该服务的账户类型、认证方式、付费门槛或准入门槛，也未标注”未验证”。
- 生产系统方案把认证、密钥、部署形态、主数据建模等地基项写进”暂不包含”，且未写明剩余风险。
- 完成标准或错误判断标准没有对应的复核动作。
- 方案会由 AI 执行，却没有把结构红线写进完成标准。
- 多需求索引缺少全局约束章节，或 R 文件未继承全局约束、未回答横切关注点检查表。

## 功能开发类补充要求

如果需求最终指向功能开发、界面实现或可交互流程交付，在收尾前补一条验收要求：

- 使用 `chrome-devtools` 按真实用户链路完成至少一轮自测。
- 根据自测结果修正关键问题后，再给出最终结论。
- Bug 修复必须先设计复现信号；重构必须先设计行为保护；新功能必须先定义验收路径。

需要查看三条核心护栏的来源和映射时，读取 [references/karpathy-principles.md](references/karpathy-principles.md)。
需要快速参考高质量输出样式时，读取 [references/examples.md](references/examples.md)。
需要做追问访谈时，读取 [references/grilling-and-clarification.md](references/grilling-and-clarification.md)。
需要把方案转成 PRD、issue、执行 brief 或 handoff 时，读取 [references/execution-bridge.md](references/execution-bridge.md)。
需要沉淀项目术语或架构决策时，读取 [references/context-and-adr.md](references/context-and-adr.md)。
需要为开发、Bug、重构补验证路径时，读取 [references/engineering-validation.md](references/engineering-validation.md)。
